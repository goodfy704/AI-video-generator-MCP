[project]
name = "ai-video-generator"
version = "0.1.0"
description = "MCP server for AI video generation"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "fastmcp==4.0.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
]